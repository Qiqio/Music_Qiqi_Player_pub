<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicqiqiplayer">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_headphone"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_headphone"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PlayMusicActivity"
            android:label="@string/title_activity_play_music"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".LocalMusicView"
            android:label="@string/home_local_music"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>